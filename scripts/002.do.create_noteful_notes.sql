CREATE TABLE noteful_notes (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  note-details TEXT,
  note-title TEXT NOT NULL,
  parent-folder INTEGER REFERENCES noteful-folders(id) ON DELETE CASCADE NOT NULL,
  date_created TIMESTAMPTZ DEFAULT now() NOT NULL
)

/*IS MY CASCADE IN THE CORRECT PLACE? IF 
THE FOLDER IS DELETED, I WANT THE CORRESPONDING NOTES TO BE DELETED*/